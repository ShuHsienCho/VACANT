% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_pareto_anchors.R, R/vacant_helpers.R
\name{find_pareto_anchors_optimized}
\alias{find_pareto_anchors_optimized}
\title{Efficiently find Pareto Anchors (Lower-Left Frontier)}
\usage{
find_pareto_anchors_optimized(points)

find_pareto_anchors_optimized(points)
}
\arguments{
\item{points}{Numeric matrix of points (e.g., scores of risk cluster variants).}
}
\value{
A matrix of anchor points defining the lower-left boundary,
subset from the input \code{points}.

A matrix of anchor points defining the lower-left boundary,
subset from the input \code{points}.
}
\description{
This helper function implements a "Sort-and-Scan" algorithm to identify
non-dominated points (Pareto frontier) in O(N log N) or O(N * k) time.

This helper function implements a "Sort-and-Scan" algorithm to identify
non-dominated points (Pareto frontier) in O(N log N) or O(N * k) time.
}
